#include<stdio.h>
int main(){
	printf("%lu bytes",sizeof(int)); //size of is a fuction to calculate size of datatypes
							/*lu is unsigned long, sizeof(int) mean size of datatype int, 
							you can place another datatype like float, char in replace of int */
	return 0;
}